{% extends "layout.html" %}

{% block content %}

<table class='table table-hover'>
  <tr>
    <th>Name</th>
    <th>Token</th>
  </tr>
  {% for person in people %}
    <tr>
      <td>{{ person.name }}</td>
      <td>{{ person.token }}</td>
    </tr>
  {% endfor %}
</table>

{% endblock %}
